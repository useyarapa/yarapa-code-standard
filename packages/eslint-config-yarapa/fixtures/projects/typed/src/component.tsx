export function Component(
  param: unknown,
  params: unknown,
  prev: unknown,
  prop: unknown,
  props: unknown,
  ref: unknown,
  refs: unknown,
): unknown[] {
  return [param, params, prev, prop, props, ref, refs];
}
